<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/root_view"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/city_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/content_description_country_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        style="@style/CityImageView" />

    <TextView
        android:id="@+id/city_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/city_image"
        app:layout_constraintRight_toRightOf="parent"
        style="@style/TextTitleView" />

    <TextView
        android:id="@+id/city_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/city_title"
        app:layout_constraintLeft_toRightOf="@id/city_image"
        app:layout_constraintRight_toRightOf="parent"
        style="@style/TextDescriptionView" />

</android.support.constraint.ConstraintLayout>